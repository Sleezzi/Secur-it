import { Pages } from "../../interfacies";

const page: Pages = {
	method: "WS",
	name: "messages/remove",
	async execute(client, message, value) {
		try {
			
		} catch (err) {
			console.error(err);
		}
	},
}

module.exports = page;