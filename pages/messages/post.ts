import { Pages } from "../../interfacies";

const page: Pages = {
	method: "WS",
	name: "postMessage",
	async execute(client, message, value) {
		try {
			
		} catch (err) {
			console.error(err);
		}
	},
}

module.exports = page;