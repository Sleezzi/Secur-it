import { Pages } from "../../../interfacies";

const page: Pages = {
	method: "WS",
	async execute(client, username, message, reply, send) {
		try {
			
		} catch (err) {
			console.error(err);
		}
	},
}

module.exports = page;